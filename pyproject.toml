[project]
name = "rapidity"
version = "0.1.0"
description = "Numerics for quantum integrable models"
requires-python = ">=3.9"
license = {text = "MIT"}
dependencies = [
    "numpy",
    "scipy",
]

[tool.ruff]
per-file-ignores = {"docs/source/conf.py" = ["E402"]}